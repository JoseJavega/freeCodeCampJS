Uso de Select como formateador de fecha

* Uso del objeto `Date()` y sus métodos `.getDate` `.getMonth` `.getFullyear` `.getHours` `.getMinutes`
* Métodos de arrays y strings `split()` `reverse()` `join()`
* Estructura de `switch` y `case`
